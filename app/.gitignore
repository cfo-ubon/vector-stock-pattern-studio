# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-desktop
dist-electron
release
*.local

# Playwright Test's own run artifacts (traces/screenshots on failure) —
# reproducible any time via `npm run test:e2e`, not source.
test-results/
playwright-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Portfolio Manager P2.5 Sprint 1 — generated validation reports
# (`npm run validate:collections*`). Reproducible any time; only a small,
# hand-curated baseline summary is committed, in
# docs/portfolio/P2_5_PERFORMANCE_BASELINE.md, not raw JSON/MD dumps.
validation-results/
