{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "palette.schema.json",
  "title": "Palette",
  "description": "One curated color palette. Mirrors app/src/palettes/palettes.ts's Palette TypeScript type — the color-roles/palettes.json data file is a JSON mirror of that same source data for the new JSON-first architecture to consume independently (see Phase 2 recommendation about wiring palettes.ts to read from this file instead of maintaining two copies).",
  "type": "object",
  "required": ["id", "label", "colors"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "label": { "type": "string", "minLength": 1 },
    "colors": { "type": "array", "minItems": 2, "items": { "type": "string", "pattern": "^#[0-9A-Fa-f]{6}$" } }
  }
}
