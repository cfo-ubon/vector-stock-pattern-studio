/// <reference types="vite/client" />

/** AI-SBOS, Part 2 — injected by `vite.config.ts`'s `define` at build time,
 * the real short commit hash the build was produced from. */
declare const __COMMIT_HASH__: string;
